name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtk-3-dev libayatana-appindicator3-dev libxdo-dev libxkbcommon-dev \
            libx11-dev libxcb1-dev libxcb-render0-dev libxcb-shape0-dev libxcb-xfixes0-dev

      - run: cargo fmt --check
      - run: cargo test
      - run: cargo build --release

      - name: Package (macOS)
        if: runner.os == 'macOS'
        run: |
          ./packaging/macos/bundle.sh
          ditto -c -k --keepParent target/MultimPaste.app MultimPaste-macos.zip

      - name: Package (Linux)
        if: runner.os == 'Linux'
        run: |
          install -D target/release/multimpaste dist/multimpaste
          install -D packaging/linux/multimpaste.desktop dist/multimpaste.desktop
          tar -czf MultimPaste-linux.tar.gz -C dist .

      - name: Package (Windows)
        if: runner.os == 'Windows'
        run: Compress-Archive -Path target/release/multimpaste.exe -DestinationPath MultimPaste-windows.zip

      - uses: actions/upload-artifact@v4
        with:
          name: MultimPaste-${{ runner.os }}
          path: |
            MultimPaste-*.zip
            MultimPaste-*.tar.gz
          if-no-files-found: ignore

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            MultimPaste-*.zip
            MultimPaste-*.tar.gz
